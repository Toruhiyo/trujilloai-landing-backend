{
    "type": "object",
    "properties": {
        "query": {
            "type": "string",
            "description": "The PostgreSQL query generated from the natural language question."
        },
        "explanation": {
            "type": "string",
            "description": "A brief explanation of what the query does and how it answers the user's question."
        }
    },
    "required": [
        "query"
    ]
}